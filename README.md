# LogScanner

LogScanner is a gem for scanning and analyzing log files.

## Installation

Add this line to your application's Gemfile:

```ruby
gem 'log_scanner'
```

And then execute:

    $ bundle install

Or install it yourself as:

    $ gem install log_scanner

## Usage

```ruby
require 'log_scanner'

# Your code here
```
